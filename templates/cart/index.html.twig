{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    <table>
        <th>Nom du produit</th>
        <th>Quantité</th>
        <th>Prix</th>

            {% for products in product %}
            <tr>
                <td>{{products.name}}</td>
                <td>1</td>
                <td>{{products.price}} €</td>
                <td><a href="{{ path('cart.delete', {id : products.id}) }}">Supprimer</a></td>
                {# <td><button class="btn btn-primary" id="delete" href="{{ path('cart.delete', {id : product.id}) }}">Supprimer</button></td> #}

            </tr>

            {% endfor %}
    
    </table>


    {{ form_start(commandForm) }}
        {{ form_row(commandForm.email) }}

        {# <button type="submit" class="btn btn-primary btn-block">
            Créer l'article
        </button> #}
    {{ form_end(commandForm) }}

{% endblock %}
